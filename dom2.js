'use strict';

const texts = ['One', 'Two', 'Three'];

// const list = document.querySelector('.topics');

// const newItemText = 'AnotherFor';

// const newItem = document.createElement('li');
// newItem.className = 'topic';
// newItem.textContent = newItemText;

// list.insertAdjacentElement('beforeend', `
//   <li class="topic">
//   ${newItemText}
//   </li>
// `);
// insertAdjacentElement - позволяет добавлять элемент на страницу прямо в разметку , при этом сохраняя все элементы, которые были до этого  на странице

{/*
list.append(newItem);  // add in end of list 
list.prepend(newItem);  // add in start of list 
//Все методы добавления удаляют элемент с предыдущего места

list.insertBefore(newItem, list.children[4]);
list.children[0].before(newItem);
list.children[2].after(newItem);

newItem.remove();
list.append(newItem);
list.append(newItem.cloneNode(true)) // add twice
//cloneNode  - copy text without content
//cloneNode(true)  - copy text with content



// rootElement.innerHTML = `
//   <ul class="topics">
//    ${texts.map(text => `
//     <li class='topic'>
//       ${text}
//       <span>*</span>
//     </li>
//    `).join('')}
//   </ul>
// `;

// const list = document.createElement('ul');
// list.className = 'topics';
// rootElement.append(list);

// for (let text of texts ) {
//   const item = document.createElement('li');
//   item.className = 'topic';
//   item.textContent = text;

//   list.append(item);
// }

*/}

